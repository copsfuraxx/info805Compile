package fr.usmb.m1isc.compilation.tp;

public enum TypeNode {
    PV,
    LET,
    NONE,
    VALUE,
    VAR,
    OPERATOR,
    COMPARATOR,
    WHILE,
    IF_THEN_ELSE,
    OUTPUT,
    INPUT,
    IF_THEN,
    THEN_ELSE,
    MOINS_UNAIRE
}
